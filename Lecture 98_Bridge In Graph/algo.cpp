// What is Bridge
// A edge if we remove it from the graph it divides the graph into two different components

#include<bits/stdc++.h>
#define ll long long
#define llv long long v
using namespace std;

class Solution{

    public:
        void solve(){
            ios_base::sync_with_stdio(0);
            cin.tie(0);
            cout.tie(0);
            
            
        }
};

int main()
{
    Solution sl;
    sl.solve();
    return 0;
}